name: sample

ctx:
  sh:
    type: shell
    args:
      envs:
        CTX_ENV_KEY: ctx-env-val
  jsontest:
    type: http
    args:
      endpoint: http://echo.jsontest.com/
    req:
      method: GET
#  imm:
#    type: pop
#    args:
#      AccessKeyId: ${POP_AK}
#      AccessKeySecret: ${POP_SK}
#      Endpoint: ${POP_ENDPOINT}
#      Method: POST
#      ProductId: immv2
